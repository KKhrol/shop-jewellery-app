syntax = "proto3";

package inventory;

service InventoryController {
    rpc FindOne (InventoryByItemId) returns (Inventory);
}

message InventoryByItemId {
    string id = 1;
}

message Inventory {
    int32 quantity = 1;
}