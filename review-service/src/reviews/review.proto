syntax = "proto3";

package review;

service ReviewsController {
    rpc FindOne (ReviewByItemId) returns (Review);
    rpc DeleteOne (ReviewByItemId) returns (DeleteResponse);
}

message ReviewByItemId {
    string id = 1;
}

message Review {
    int32 voters = 1;
    double mark = 2;
}

message DeleteResponse {
    string message = 1;
}